/* Activates magnific popup
------------------------------------------------------ */
jQuery(window).load(function(){
	jQuery('.open-popup-link a').magnificPopup({
	  type:'inline',
	  midClick: true
	});
});